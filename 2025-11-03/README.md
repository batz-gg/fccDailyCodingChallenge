# Үг тоолуур

Өгүүлбэрийн мөр өгөгдсөн бол өгүүлбэрт байгаа үгсийн тоог буцаана уу. Үг нь хоосон зайгүй тэмдэгтүүдийн дурын дараалал бөгөөд нэг хоосон зайгаар тусгаарлагддаг.

## Tests

* **Passed:**1. `countWords("Hello world")` should return `2`.
* **Passed:**2. `countWords("The quick brown fox jumps over the lazy dog.")` should return `9`.
* **Passed:**3. `countWords("I like coding challenges!")` should return `4`.
* **Passed:**4. `countWords("Complete the challenge in JavaScript and Python.")` should return `7`.
* **Passed:**5. `countWords("The missing semi-colon crashed the entire internet.")` should return `7`.
